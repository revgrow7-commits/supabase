# Migrations legadas — NÃO aplicar

Estes arquivos são do schema original anterior à reestruturação.
O schema autoritativo é `../001_schema_completo.sql` + patches `002`, `003`, `004`.

Aplicar estes arquivos em qualquer ambiente novo vai criar divergência de schema.
